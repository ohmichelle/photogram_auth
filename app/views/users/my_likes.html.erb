<div class="page-header">
  <h3><%= current_user.username %></h3>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>Photos</th>
      </tr>


      <% @user_likes.each do |photo| %>
      <tr>
        <td><%= photo.caption %></td>
        <td><a href="/photos/<%= photo.id %>"><img src="<%= photo.image %>" width="48"></td>
        <td><%= photo.user_id %></td>
      </tr>
      <% end %>
    </table>
  </div>
</div>
